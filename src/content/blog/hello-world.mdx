---
title: Hello, world
description: A placeholder Post that exercises the blog collection until the first real one is written.
pubDate: 2026-09-24
tags: [meta]
---

This is a placeholder. It exists so the `blog` collection has something to
validate, and will be replaced by the first real Post.

```ts
const greeting: string = 'hello, world';
```

Embeds render from their committed Archive, with no network call at build time:

<Tweet id="20" />

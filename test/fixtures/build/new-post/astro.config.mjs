// The real site built against a Post generated by the new-post script, which
// test/build.test.ts writes into ./src/content/blog before building.
import config from '../../../../astro.config.mjs';

export default { ...config, srcDir: '../../../../src' };
